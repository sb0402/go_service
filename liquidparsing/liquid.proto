
syntax = "proto3";

package liquidgrpc;
option go_package = "liquidparsing/liquidparsingpb";

service LiquidParsingGrpc {
    rpc ParseAndRenderString(Data) returns (ParsedResult) {}
}

message Data {
  map<string, string> variables = 1;
}
message ParsedResult {
  string result = 1;
}